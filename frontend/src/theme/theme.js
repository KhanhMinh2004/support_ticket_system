import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        secondary: {
            main: '#ffffff'
        },
        background:{
            default: '#fafafa'
        }
    }
})
export default theme;